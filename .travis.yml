sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t krishnakotadocker/docker-demo -f Dockerfile.dev .

script:
  - docker run -e CI=true krishnakotadocker/docker-demo npm run test
